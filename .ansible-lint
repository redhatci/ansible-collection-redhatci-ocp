---
# vim: ft=yaml

rulesdir:
  - hack/rules

use_default_rules: true

display_relative_path: true

verbosity: 1

exclude_paths:
  - .github
  - plugins
  - hack
  - tests

skip_list:
  - var-naming[no-role-prefix]

warn_list:
  - ignore-errors
  - no-changed-when
  - yaml[line-length]
...
